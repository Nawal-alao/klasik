# Copier ce fichier en `.env` pour le frontend (lu par Vite).
# Ne JAMAIS committer de vraies valeurs.

# Développement : laisser vide → le proxy Vite (/api → 127.0.0.1:8000) est utilisé.
# Production (Vercel) : URL du backend Render, SANS slash final.
# VITE_API_URL=https://evoly-api.onrender.com
