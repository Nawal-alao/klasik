# Copier ce fichier en `.env` puis renseigner les vraies valeurs.
# Ne JAMAIS committer de vraies clés ou mots de passe ici.

DJANGO_SECRET_KEY='django-insecure-CHANGER-MOI'
COHERE_API_KEY='VOTRE_CLE_API_COHERE'

# Optional app/runtime settings already used by the project
DEBUG=True
ALLOWED_HOSTS='*'
EMAIL_BACKEND='django.core.mail.backends.console.EmailBackend'

# Database — PostgreSQL (par défaut)
DB_NAME='evoly_db'
DB_USER='evoly_user'
DB_PASSWORD='MOT_DE_PASSE_A_CHANGER'
DB_HOST=localhost
DB_PORT=5432

# Développement local avec SQLite à la place :
# DB_ENGINE='django.db.backends.sqlite3'
